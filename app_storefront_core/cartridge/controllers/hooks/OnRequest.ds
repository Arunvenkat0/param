/**
 * The OnRequest pipeline is called with every request in site. This happens both for cached and non-cached pages. For performance reasons the pipeline should be kept short.
 */
function Do()
{
}

/*
 * Module exports
 */

/*
 * Local methods
 */
exports.Do = Do;
