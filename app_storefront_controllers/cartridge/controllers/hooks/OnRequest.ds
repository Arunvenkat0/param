'use strict';
/**
 * The OnRequest pipeline is called with every request in site. This happens both for cached and non-cached pages.
 * For performance reasons the pipeline should be kept short.
 *
 * @module  controllers/hooks/OnRequest
 */

/**
 * The on request handler
 */
exports.Do = function (){
};
